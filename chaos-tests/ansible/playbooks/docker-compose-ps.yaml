---
- hosts: all:!spammer*
  gather_facts: no
  tasks:
  - name: get docker-compose ps info
    shell: |
      cd /home/ubuntu/symbol-testnet/{{type}}
      docker-compose ps
    register: result
  - debug:
      var: result

